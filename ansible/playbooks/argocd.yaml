---
# Runs locally and requires Helm + kubectl configured on the development machine
- name: install argocd
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  roles:
    - argocd