apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: renovate-config
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/MosheAzraf/proxmox-iac-k3s-lab
    targetRevision: main
    path: _kubernetes/platform/renovate/configs

  destination:
    server: https://kubernetes.default.svc
    namespace: renovate

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true