apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pgadmin
  namespace: pgadmin
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`pgadmin.home.lab`)
      kind: Rule
      services:
        - name: pgadmin
          port: 80
  tls:
    secretName: pgadmin-tls