apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homarr-certificate
  namespace: homarr
spec:
  secretName: homarr-tls
  dnsNames:
    - homarr.home.lab
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer